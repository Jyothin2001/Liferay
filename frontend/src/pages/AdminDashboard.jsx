import React,{useEffect,useState} from 'react'
import { api } from '../api'
export default function AdminDashboard(){const [users,setUsers]=useState([]);const [title,setTitle]=useState('');const [content,setContent]=useState('');useEffect(()=>{api.get('/users').then(r=>setUsers(r.data)).catch(()=>setUsers([]))},[]);function addPost(e){e.preventDefault();api.post('/posts',{title,content}).then(()=>{setTitle('');setContent('');alert('Post created')}).catch(()=>alert('error'))}return(<div className='container layout'><div className='admin-sidebar'><h3>Admin</h3><div>Users: {users.length}</div></div><div style={{flex:1,paddingLeft:16}}><h2>Dashboard</h2><form onSubmit={addPost} className='card'><input value={title} onChange={e=>setTitle(e.target.value)} placeholder='Title' style={{width:'100%',padding:8,marginBottom:8}}/><textarea value={content} onChange={e=>setContent(e.target.value)} placeholder='Content' style={{width:'100%',padding:8,marginBottom:8}}/><button className='btn'>Create Post</button></form></div></div>)}